version: '3.8'

services:
  shadowmixer:
    build: .
    ports:
      - "8080:8080"
    depends_on:
      - redis
    environment:
      - REDIS_ADDR=redis:6379
    volumes:
      - ./config.yaml:/app/config.yaml

  redis:
    image: redis:alpine
    ports:
      - "6379:6379"
